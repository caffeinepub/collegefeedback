{
  "frontend/src/index.css": "Global styles importing Bricolage Grotesque (headings) and DM Sans (body) from Google Fonts, with the full OKLCH color palette, and a fixed dot-grid background pattern applied to the body using CSS radial-gradient with a soft lavender/violet tint.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Bricolage Grotesque as the heading/display font family and DM Sans as the body/sans font family, replacing the previous Nunito/Sora/Inter setup, plus all existing color tokens, border-radius, shadows, and animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — getAllPosts, getPostsByCategory, getPost, getStats, and createPost; useGetStats and useGetAllPosts now use aggressive retry (up to 10 attempts with exponential backoff) and refetchInterval polling (every 3s until data is available) so the Dashboard auto-recovers without manual user interaction.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header now including a Bell/BellOff mute toggle button that reads/writes mute state via the sounds utility, alongside the existing language toggle, pill navigation, year badge, and a three-column footer.",
  "frontend/src/components/PostCard.tsx": "Post card with emoji category badges, clickable card linking to /post/:id, and read-only display of upvote and connect counts (no mutation buttons since backend doesn't support those operations); exports categoryConfig and formatTimeAgo utilities.",
  "frontend/src/pages/Home.tsx": "Home feed page with hero banner, trending section, and category filter tabs — uses correct existing translation keys (catAll, heroBadge, failedLoad, noPostsTitle, noPostsDesc, shareExpBtn), passes a valid Category to useGetPostsByCategory by falling back to Category.general when 'all' is selected (the 'all' tab uses allPostsQuery data instead), and calls playBubblePop() on each tab click.",
  "frontend/src/pages/ShareExperience.tsx": "Share form with live character counter and category selector — now calls playTypingTick() on each keydown in the textarea and playBubblePop() on successful form submission via the sounds utility.",
  "frontend/src/App.tsx": "TanStack Router setup with five routes (/, /share, /post/:id, /dashboard, /about) wrapped in a year-gate that shows YearSelectionModal until a year is selected.",
  "frontend/src/hooks/useYearSelection.ts": "A lightweight hook that reads/writes the selected college year (1st–4th Year) from sessionStorage and exposes the current year, a setter, and a hasSelected boolean.",
  "frontend/src/components/YearSelectionModal.tsx": "Full-screen blocking modal with Space Grotesk headings, emoji year buttons, and a college name optional text input placed beneath the year selection buttons; on confirmation stores both the selected year and college name via useYearSelection.",
  "frontend/src/pages/PostDetail.tsx": "Full post detail page showing content, stats, share bar, and CTA — the upvote and connect stat buttons now call playBubblePop() on click via the sounds utility.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page at /dashboard showing hero stat cards, category breakdown, top posts lists, and year distribution; now uses automatic retry/polling from useGetStats with a skeleton + spinner loading state, a non-crashing auto-retrying error fallback (no manual reload button), and a subtle live-stats spinner while background refetches occur.",
  "frontend/src/pages/About.tsx": "About page at /about with a responsive banner image, three sections (What is SeniorCompass, How It Works with 4 emoji steps, Why It Matters with value cards and a quote), and a CTA section linking to Feed and Share.",
  "frontend/src/utils/sounds.ts": "Web Audio API utility module exporting playBubblePop() (a pleasant tone burst) and playTypingTick() (a crisp noise tick), plus getMuted()/setMuted() helpers that persist mute state to sessionStorage, with graceful fallback for unsupported browsers."
}